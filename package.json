{
  "name": "castroom-ai",
  "version": "0.1.3",
  "private": true,
  "license": "GPL-3.0-only",
  "repository": {
    "type": "git",
    "url": "https://github.com/98k4jd2qgt-ship-it/CastRoom-AI.git"
  },
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173 --strictPort",
    "build": "tsc && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "serve:dist": "node scripts/serve-dist.mjs",
    "icons:placeholder": "node scripts/generate-placeholder-icons.mjs",
    "pack:validate": "node scripts/pack-tool.mjs validate",
    "pack:inspect": "node scripts/pack-tool.mjs inspect",
    "tauri": "tauri",
    "check": "tsc --noEmit",
    "check:rust": "scripts\\check-rust.cmd"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
